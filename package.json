{
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "spellcheck": "./frontend/node_modules/.bin/cspell --config cspell.json .",
    "test:guards": "vitest --run",
    "test:js-resolvers": "cd tofu/application/appsync/js-resolvers && node --import ./register-loader.mjs --test --test-concurrency=1",
    "test": "npm run test:js-resolvers"
  },
  "devDependencies": {
    "@aws-sdk/client-appsync": "^3.955.0",
    "@aws-sdk/client-cloudformation": "^3.955.0",
    "jsdom": "^27.3.0",
    "vitest": "^4.0.15"
  },
  "dependencies": {
    "@playwright/mcp": "^0.0.54"
  }
}
